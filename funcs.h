#include <iostream>
#include <cctype>

using std::string;

string removeLeadingSpaces(string line);

int countChar(string line, char c); //checking

string unindent(string line);  //I added this

string indent(); //indent() recieves errors
